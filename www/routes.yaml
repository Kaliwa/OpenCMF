/:
  controller: Main
  action: home

/profile:
  controller: User
  action: profile

/contact:
  controller: Main
  action: contact

/login:
  controller: Security
  action: login
  
/logout:
  controller: Security
  action: logout

/register:
  controller: Security
  action: register

/refering:
  controller: Main
  action: refering

/portfolio:
  controller: Portfolio
  action : portfolio

/project:
  controller: Portfolio
  action: project

/page:
  controller: Portfolio
  action: page