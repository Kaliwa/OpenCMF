/:
  controller: Main
  action: home

/profile:
  controller: User
  action: profile

/contact:
  controller: Main
  action: contact

/login:
  controller: Security
  action: login
  
/logout:
  controller: Security
  action: logout

/register:
  controller: Security
  action: register

/refering:
  controller: Main
  action: refering

/portfolio:
  controller: Portfolio
  action : portfolio

/project:
  controller: Project
  action: project

/page:
  controller: Page
  action: page